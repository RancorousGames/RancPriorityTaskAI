# RancPriorityTaskAI Release Instructions

This plugin has a separate branch for releases called `origin/ReleaseWithSamples`.
Before every release, this branch should be rebased on `main`.
Any changes (e.g., version upgrades) made during the release process need to be committed to `main` first.
After releasing, switch back to the `main` branch.
